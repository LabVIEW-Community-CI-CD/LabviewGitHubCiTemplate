Bootstrap is complete enough that this issue should stop carrying the active queue by itself.

What landed so far:
- canonical repo bootstrapped on `develop`
- org + personal forks created and issue-enabled
- cookiecutter + GitHub-template semantics proven
- hosted Linux + Windows template-smoke workflow green
- generated consumer now includes `AGENTS.md`, issue templates, and proving-rail docs

I am splitting the next work into narrower implementation lanes so the template queue stays actionable.
